{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://regurgitator.emarte.uk/test-schema.json",
  "title": "Test",
  "description": "A test json schema",
  "type": "object",
  "properties": {
    "testId": {
      "description": "The unique identifier for a test",
      "type": "string"
    }
  },
  "required": [ "testId" ]
}
